//If we need we can use custom property
tailwind.config = {
    theme: {
      extend: {
        colors: {
          extraLightBlue: '#6F8BC2',
          extraLightGray: '#F6F9FF'
        }
      }
    }
}